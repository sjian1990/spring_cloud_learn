server:
  port: 8093

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8092/eureka/
      

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic      